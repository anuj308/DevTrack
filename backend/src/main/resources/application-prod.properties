# Production profile - Supabase PostgreSQL (Connection Pooling)
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres}
spring.datasource.username=${DB_USERNAME:postgres.ynnbckjpcysbgdkkfuxo}
spring.datasource.password=${DB_PASSWORD:mascssco308}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration (use environment variable in production)
app.jwt.secret=${JWT_SECRET:your-super-secret-key-change-this-in-production-at-least-256-bits}
app.jwt.expiration=${JWT_EXPIRATION:86400000}
